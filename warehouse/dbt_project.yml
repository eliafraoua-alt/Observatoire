name: 'observatoire_rpdf'
version: '1.0.0'
config-version: 2

profile: 'observatoire_rpdf'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  observatoire_rpdf:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
